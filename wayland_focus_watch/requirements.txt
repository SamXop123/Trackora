# PyGObject provides gi.repository.Gio (D-Bus) and gi.repository.Atspi (AT-SPI2).
# Fedora: `sudo dnf install python3-gobject python3-cairo-devel` (and typically
# `at-spi2-core` is already installed with GNOME).
PyGObject>=3.42.0
